---
kind: exam
company: "{{company}}"
round: "{{round_name}}"
interviewer: "{{interviewer}}"
datetime: "{{iso8601_with_offset}}" # source of truth for the T−4h freeze
language: "{{ru|en}}"
scope_source: "{{employer document, quoted description, or `recollection`}}"
sources:
  - "{{canonical material — author, title, chapters}}"
status: prep # prep | ready | done
---

# {{company}} · {{round_name}} — экзаменационный раунд

Источник скоупа: `{{scope_source}}`. Строки таблицы — дословно оттуда.
Прогонный датасет на весь раунд: **{{running dataset — three input numbers}}**

## Программа

| #   | Тема (дословно из scope_source) | Источник (раздел) | Прогонный кейс | Статус   |
| --- | ------------------------------- | ----------------- | -------------- | -------- |
| 1   |                                 |                   |                | не начат |

Статусы: `не начат` → `объяснён` → `отработан вслух`. Статус переводится в
момент перехода, не в конце сессии. Строку из scope_source нельзя удалить;
добавленный пререквизит помечается `+ prerequisite`. Пустая ячейка «Источник»
или `мой синтез` = надёжность ниже, кандидат предупреждён.

Готовность = дробь по этой таблице. Не «85% на глаз».

## Долги

| id  | Дата | Долг | Почему всплыл | Закрыт |
| --- | ---- | ---- | ------------- | ------ |

Строка не удаляется — ей проставляется `Закрыт: <дата> — проговорено вслух`.
id сквозные: пропуск в нумерации = кто-то удалил строку.

## Мок

- Дата:
- Покрытый формат (сверить с `scope_source`):
- Вопросы только по пройденному материалу: да / нет
- Follow-up давление применялось: да / нет

## Readiness DoD

- [ ] все строки программы `отработан вслух`
- [ ] открытых долгов нет
- [ ] у каждой темы есть запись в шпаргалке, схема валидна
- [ ] мок покрыл формат из `scope_source`
- [ ] стоп-линия T−4h соблюдена

Слова «готов» и «100%» запрещены, пока чеклист не пройден. Финальный вердикт
о готовности — за кандидатом.

## Инциденты

Пишутся в `{{same-name}}_INCIDENTS.md` в момент поломки, blameless.
